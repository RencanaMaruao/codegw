SAVEFDED


Setelah project di save : Run 
1. git add . (add all perubahan)
2. git commit -m "Task apa"
3. git push -u origin main

Step by step git add .
             git status
             git add
             git status
             git commit -m "new"
             git status
             git log
             git push -u origin main